specVersion: "v1beta"
serviceId: "marathon-training-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "marathon-training-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
